{
  "name": "caregiver-root",
  "version": "1.0.0",
  "description": "Root package.json to route Render deployment to the backend folder",
  "scripts": {
    "postinstall": "cd backend && npm install && npx prisma generate && npx tsc",
    "start": "cd backend && npm start"
  }
}
